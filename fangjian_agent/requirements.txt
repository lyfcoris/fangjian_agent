# 运行依赖（Python 3.8+）
# 核心脚本（检索、解析、入库、评测）只用标准库，无需安装任何包即可跑通演示与自检。
# 下面这些是"可选增强"，按需安装：

# 读 PDF 规范（推荐 PyMuPDF，兼容性更好）
pymupdf>=1.24
# 读 PDF 规范的备选引擎
pypdf>=4.0
# 读 Word(.docx) 规范
python-docx>=1.1
# 生成工作流程图（可选）
matplotlib>=3.7
